# ============================================================
# 提示词模板 — 告诉 DeepSeek 如何回答问题
#
# 提示词决定了回答的质量和风格
# 关键设计：
#   · 只根据参考资料回答（拒绝幻觉）
#   · 标注引用来源（可追溯）
#   · 无法回答时明确说"不知道"
# ============================================================

# ── 系统提示词 ──
# 这个提示词会在每次对话时发给 DeepSeek
# 相当于给模型设定的"角色"和"行为规则"
SYSTEM_PROMPT = """你是一个企业知识库助手，请基于提供的参考资料准确回答问题。

## 回答原则
1. **依据优先**：只根据参考资料回答，资料不足时直接说明"根据现有资料无法完全回答"。
2. **引用标注**：引用时用上标标注编号，例如 ^[1]^ 或 ^[2]^。
3. **精炼准确**：答案简洁、结构化，避免冗余。
4. **中文回答**：默认用中文，除非用户用其他语言提问。
5. **拒绝幻觉**：绝对不能编造资料中没有的信息。
"""


def build_qa_prompt(query: str, context: str) -> list[dict]:
    """构建完整的 Prompt 消息列表。
    
    参数：
        query: 用户的问题
        context: 检索到的参考资料文本
    返回：
        messages 列表，直接发给 DeepSeek API
    """
    return [
        {"role": "system", "content": SYSTEM_PROMPT},  # 系统指令
        {"role": "user", "content": f"## 参考资料\n\n{context}\n\n## 问题\n\n{query}"},
        # user 消息包含两部分：
        #   ## 参考资料 — 检索到的相关文本
        #   ## 问题 — 用户实际问的内容
    ]
