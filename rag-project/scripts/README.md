# 脚本说明

| 命令 | 作用 | 使用方法 |
|------|------|----------|
| `setup.ps1` | 初始化开发环境 | `.\scripts\setup.ps1` |
| `dev.ps1` | 启动后端服务器 | `.\scripts\dev.ps1` |
| `lint.ps1` | 运行代码检查 (ruff + mypy) | `.\scripts\lint.ps1` |
| `test.ps1` | 运行测试 (pytest) | `.\scripts\test.ps1` |

所有脚本应在项目根目录执行。
