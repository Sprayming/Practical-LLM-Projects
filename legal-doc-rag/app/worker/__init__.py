"""
app.worker —— 后台 Worker 子包（包标识文件）

【作用与功能】
提供两类后台执行能力：其一是基于优先级队列的"影子 Worker"（无阻塞后台任务执行器，
用于可延迟、可重试的轻量任务）；其二是 Webhook 通知系统（将系统事件异步推送到外部
订阅地址）。

【实现方式】
本文件仅作为包标识，不承载运行逻辑。具体能力由子模块提供：
- `shadow_worker`:优先级任务队列、多 Worker 线程、自动重试，通过 `get_worker()` 获取全局单例
- `webhook`:Webhook 配置管理、事件触发、签名校验与失败重试，通过 `get_webhook_manager()` 获取全局单例

【整体作用】
让业务代码提交可延迟任务（如索引、通知）而不阻塞请求线程，是应用后台异步能力的承载层。
"""
