"""
app.memory —— 记忆系统子包

【作用与功能】
本子包实现 legal-doc-rag 的多层记忆能力，用于在多轮对话中保留用户上下文、
画像信息与长期知识。涵盖对话历史存储、记忆遗忘（衰减/裁剪）、用户画像存取，
以及基于 Redis 的缓存层，支撑问答接口的上下文拼接与个性化回答。

【主要组成】
- `conversation_store`：对话历史与上下文的持久化存取。
- `memory_manager`：记忆系统的统一编排入口（记忆写入/读取/总结）。
- `forgetting`：记忆遗忘策略（时间衰减、容量裁剪等）。
- `profile_store`：用户画像信息的读写。
- `redis_client`：Redis 缓存客户端封装（缺失时自动降级为内存）。

【适用场景】
- 问答接口需要拼接历史对话与用户画像以提供连贯回答时。
- 需要按租户隔离、并支持缓存加速的记忆读写场景。

【依赖关系】
- 上游调用方：app.api.chat、app.memory.memory_manager。
- 下游依赖：Redis（可选）、嵌入模型（记忆向量化）。
"""
