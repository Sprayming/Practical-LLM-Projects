"""
app.memory —— 多层记忆系统子包（包标识文件）

【作用与功能】
实现 legal-doc-rag 的多层记忆能力，在多轮对话中保留用户上下文、画像信息与长期知识，
支撑问答接口的上下文拼接与个性化回答。

【实现方式】
本文件仅作为包标识，不承载运行逻辑。具体能力由子模块提供：
- `conversation_store`:对话历史与上下文的持久化存取
- `memory_manager`:记忆系统的统一编排入口（记忆写入/读取/总结）
- `forgetting`:记忆遗忘策略（时间衰减、容量裁剪等）
- `profile_store`:用户画像信息的读写
- `redis_client`:Redis 缓存客户端封装（缺失时自动降级为内存）

【整体作用】
在多轮对话中保留并隔离（按租户）用户上下文与画像，为连贯、个性化的回答提供记忆支撑。
"""
