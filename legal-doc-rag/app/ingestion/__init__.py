"""
app.ingestion —— 法律文档入库·图片语义标注子包（包标识文件）

【作用与功能】
为文档中的图片生成语义描述（caption），配合 OCR 提取的图片文字，实现"以文搜图"能力，
是 legal-doc-rag 多模态入库链路的组成部分。

【实现方式】
本文件仅作为包标识，不承载运行逻辑。具体能力由子模块提供：
- `vision_caption`:调用 Vision LLM（如 DeepSeek）为单张/批量图片生成一句话核心描述

处理管线（`app.processing.multimodal_pipeline`）会对每页图片调用本子包，生成"[图片描述]"
文本并入块，为扫描件、图表类法律文档补充可被检索的图片语义信息。

【整体作用】
为图片检索与问答提供可被向量化的图片文本表示，补足纯 OCR 文字之外的多模态语义。
"""
