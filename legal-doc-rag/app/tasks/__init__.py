"""
app.tasks —— 异步任务子包（包标识文件）

【作用与功能】
负责文档索引异步任务的创建、状态追踪与持久化，使文档上传接口能够秒回而不阻塞主服务。

【实现方式】
本文件仅作为包标识，不承载运行逻辑。具体能力由子模块提供：
- `task_store`:文档索引任务的内存状态管理 + JSON 文件持久化 + 单 worker 后台线程池
  （`submit_indexing_job`）

【整体作用】
让文档上传接口创建索引任务并立即返回 task_id，前端可轮询进度；服务重启后从磁盘恢复
未完成任务，避免误判，是异步入库链路的状态中枢。
"""
