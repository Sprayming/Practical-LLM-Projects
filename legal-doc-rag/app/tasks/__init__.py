"""
tasks/__init__.py —— Legal-DOC-RAG 异步任务子包入口

【作用与功能】
本文件为 app/tasks 异步任务子包的包初始化文件。该子包负责文档索引
异步任务的创建、状态追踪与持久化（见 task_store.py），使文档上传接口
能够秒回而不阻塞主服务。本文件作为子包的统一导入入口，便于应用层
以 `from app.tasks import ...` 的方式使用任务存储能力。

【主要组成】
- 子模块 `task_store`：文档索引任务的内存状态管理 + JSON 文件持久化 +
  单 worker 后台线程池（submit_indexing_job）

【适用场景】
- 场景1：文档上传接口创建索引任务并立即返回 task_id
- 场景2：前端轮询任务状态时读取任务进度与结果
- 场景3：服务重启后从磁盘恢复未完成任务，避免误判

【依赖关系】
- 上游调用方：文档上传接口、chat 接口、前端轮询接口
- 下游依赖：app.tasks.task_store（实际实现）
"""
