"""
app.observability —— 可观测性子包（包标识文件）

【作用与功能】
为 legal-doc-rag 提供统一的运行可观测能力：结构化日志、调用链追踪与系统监控指标采集，
是定位性能瓶颈与线上问题的核心支撑。

【实现方式】
本文件仅作为包标识，不承载运行逻辑。具体能力由子模块提供：
- `structured_logger`:结构化日志封装（带模块标识与上下文字段）
- `tracker`:调用链/请求追踪（TraceContext），串联一次问答的完整链路
- `monitoring`:监控指标采集与健康检查（如 /health 状态、查询耗时上报）

【整体作用】
让请求链路、检索耗时、问答质量等关键信号可被记录与告警，替代散落的 print，
支撑线上问题定位与性能优化。
"""
